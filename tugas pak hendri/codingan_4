class PersegiPanjang:
    def __init__(self, panjang, lebar):
        self.panjang = panjang
        self.lebar = lebar

    def hitung_luas(self):
        return self.panjang * self.lebar

# Membuat objek
kotak = PersegiPanjang(10, 5)
print(f"Luas persegi panjang: {kotak.hitung_luas()}")
